{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "AgentSettings": {
    "BackendUrl": "https://localhost:7257",
    "ChannelCapacity": 10000,
    "ExpectedApiKey": "SuperSecretFieldAgentKey2026",
    // Interval in hours between cleanup runs
    "CleanupIntervalHours": 24,
    // Retention period in hours for Error records before deletion
    "DeleteErrorAfterHours": 168,
    // Retention period in hours for Completed records before deletion
    "DeleteCompletedAfterHours": 24
  }
}